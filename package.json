{
  "name": "decidim-goteborg",
  "version": "0.0.1",
  "description": "Tweaks for the Gothenburg Decidim instance",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "lint": "eslint -c .eslintrc.json --no-error-on-unmatched-pattern --ignore-pattern app/packs/vendor --ext .js app/packs",
    "stylelint": "stylelint app/packs/**/*.scss"
  },
  "dependencies": {
    "@decidim/browserslist-config": "^0.30.6",
    "@decidim/webpacker": "^0.30.6"
  },
  "devDependencies": {
    "@decidim/dev": "^0.30.6",
    "@decidim/eslint-config": "^0.30.6",
    "@decidim/stylelint-config": "^0.30.6"
  },
  "browserslist": ["extends @decidim/browserslist-config"]
}
